# CSE368 Project - Group 10
- The project will not run without the .env file included in the submission which contains the environment variables used.
- Must have docker desktop installed to run the server and the database.
- Use command 'docker compose up --build --force-recreate' to run the app.
